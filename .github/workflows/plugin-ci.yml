name: Plugin CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  portable-acceptance:
    name: Ubuntu ${{ matrix.ubuntu }} acceptance
    runs-on: ubuntu-${{ matrix.ubuntu }}
    strategy:
      fail-fast: false
      matrix:
        ubuntu:
          - "22.04"
          - "24.04"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: npm
          cache-dependency-path: cli/package-lock.json
      - run: npm ci --prefix cli
      - run: bash ci/portable-acceptance.sh

  container-acceptance:
    name: Ubuntu ${{ matrix.ubuntu }} container
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        ubuntu:
          - "22.04"
          - "24.04"
    steps:
      - uses: actions/checkout@v4
      - name: Build acceptance image
        run: >-
          docker build
          --file Dockerfile.acceptance
          --build-arg UBUNTU_VERSION=${{ matrix.ubuntu }}
          --tag workflow-acceptance:${{ matrix.ubuntu }}
          .
      - name: Run acceptance image
        run: docker run --rm workflow-acceptance:${{ matrix.ubuntu }}

  desktop-contract:
    name: Desktop contract on Ubuntu ${{ matrix.ubuntu }}
    runs-on: ubuntu-${{ matrix.ubuntu }}
    strategy:
      fail-fast: false
      matrix:
        ubuntu:
          - "22.04"
          - "24.04"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - run: npm ci --prefix apps/desktop
      - run: npm run check --prefix apps/desktop

  desktop-runtime-ubuntu:
    name: Desktop runtime on Ubuntu ${{ matrix.ubuntu }}
    runs-on: ubuntu-${{ matrix.ubuntu }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        ubuntu:
          - "22.04"
          - "24.04"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - run: npm ci --prefix apps/desktop
      - name: Launch Electron without the optional CLI
        run: xvfb-run --auto-servernum npm start --prefix apps/desktop
        env:
          GATEREEVE_DESKTOP_SMOKE: "1"

  desktop-runtime-macos:
    name: Desktop runtime on macOS
    runs-on: macos-14
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.17.0
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - run: npm ci --prefix apps/desktop
      - name: Launch Electron without the optional CLI
        run: npm start --prefix apps/desktop
        env:
          GATEREEVE_DESKTOP_SMOKE: "1"
