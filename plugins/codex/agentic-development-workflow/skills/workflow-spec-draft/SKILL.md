---
name: workflow-spec-draft
description: "Draft acceptance criteria and binary rubric for a software feature or task. Use when requirements are vague, a spec lacks AC/rubric, the user asks for a spec, or planning should begin from observable behavior before implementation."
---

# Workflow Spec Draft

## Portable Resource Root

Resolve `<plugin-root>` from the real path of this `SKILL.md`: it is the parent
directory of `skills/`. Replace the placeholder before opening files or running
commands, and quote the resolved path.

Read:

- `<plugin-root>/resources/policy/WORKFLOW.md`
- `<plugin-root>/resources/commands/spec-draft.md`

Draft AC and rubric from the user's feature description and repo evidence.
Present for approval before writing files unless the user explicitly asked for
file edits.

Keep DoD out of AC. DoD is global.
