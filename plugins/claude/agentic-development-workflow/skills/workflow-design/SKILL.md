---
name: workflow-design
description: "Design phase of the software-development workflow: grill-me interview, design.md synthesis, and the design gate. Use at branch start for any non-trivial feature, before any spec work."
---

Resolve `<plugin-root>` from the real path of this `SKILL.md`: it is the parent
directory of `skills/`. Replace the placeholder before opening files, and
quote the resolved path.

Read `<plugin-root>/resources/PROTOCOL.md`. Initialize a missing new feature
through the plugin protocol before beginning the interview. For a governed
feature, record explicit design approval through the semantic protocol passage
before any specification work.

Load and follow
`<plugin-root>/resources/commands/design.md`.
